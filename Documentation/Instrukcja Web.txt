Do poprawnego dzia³ania aplikacji wymagane s¹:
a) .NET Core 2.2 Runtime
b) .NET Core 2.2 SDK

Visual Studio 2017, 2019 lub inny dowolny edytor + .NET Core CLI (konsola).

Port, na którym nas³uchuje aplikacja skonfigurowany jest w pliku launchSettings.sjon

I. Uruchomienie projektu w Visual Studio 2017 lub 2019:
a) odpalenie solucji
c) mo¿liwy jest wybór jednego z trzech profilów uruchomienia
	- IIS Express
	- Docker (Docker for Windows)
	- Inzynierka (aplikacja konsolowa)
d) 
II. Uruchomienie projektu w Visual Studio Code:
a) otworzenie folderu Web z projektem
b) otworzenie terminalu i wpisanie dwóch komend:
	- dotnet build
	- dotnet run